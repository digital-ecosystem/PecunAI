import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false,
  allowedDevOrigins: ['https://ai.digital-ecosystem.management'],
};

export default nextConfig;
